import 'package:flutter/material.dart';

class Other_2_Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          //title: Text('Morse'),
          ),
      body: Container(),
    );
  }
}
