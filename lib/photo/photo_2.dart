import 'package:flutter/material.dart';

class Photo_2_Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Photo 2'),
      ),
      body: Container(),
    );
  }
}
