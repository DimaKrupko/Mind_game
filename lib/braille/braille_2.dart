import 'package:flutter/material.dart';

class Braille_2_Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Braille'),
      ),
      body: Container(),
    );
  }
}
